interface:
  display_name: "Add Public Site"
  short_description: "Onboard a site into the homelab pipeline"
  default_prompt: "Use $add-public-site to add a new public site to the homelab deployment pipeline."
